# DockerSpringBootApp
run simple spring boot app on docker container where postgres db will run on separate container and spring boot app will run on separate container and will use for this Dockerfile and Docker-compose so will able to start and stop both db using single command 
